# Verify: Marketing site and retainer enquiry · spec 0009 · updated 2026-09-06
_Steps derived from spec 0009 acceptance criteria. `/check verify` runs these; `/test` locks the durable ones. Local stack: `supabase start`, `pnpm dev`, `pnpm trigger:dev` from this checkout (a worker from another checkout does not know the `enquiry_received` template), Mailpit at http://127.0.0.1:54324._

## UI / manual
- [x] Open `/en`, `/en/pricing`, `/en/about`, `/en/contact`, `/de`, `/de/preise`, `/de/ueber-uns`, `/de/kontakt` → each renders with one `h1`, a localized `<title>`, the canonical link of its own language and three `hreflang` links (`de-CH`, `en-CH`, `x-default` pointing at English); `/de/pricing` and `/en/preise` answer 404 → AC-1 (rerun 2026-09-06 by /check verify on the dev server: all eight pages answer 200 with exactly one `h1`, a localized `<title>` ("Preise · SME24", "Pricing · SME24" and so on), the canonical of their own language and all three `hreflang` links with the German slugs and `x-default` on English. One wording correction: `/de/pricing` and `/en/preise` do not answer 404, they answer 307 to `/de/preise` and `/en/pricing`. That is next-intl canonicalising a wrong language slug and it is what `e2e/marketing.spec.ts` asserts, so the behaviour is right and this line's "404" was the drift.)
- [x] View the source of `/de/preise` → `og:title`, `og:description`, `og:url`, `og:site_name`, `og:locale=de_CH`, `og:locale:alternate=en_CH`, `og:type=website`, `og:image` (1200 by 630) with `og:image:alt` = "Fixpreis. Keine Überraschungen.", `twitter:card=summary_large_image`; open the `og:image` URL → `image/png`, 1200 by 630, the statement in Geist Bold on black with the mark → AC-2
- [ ] Copy each page's `application/ld+json` script into the Schema.org validator and Google's Rich Results Test → `Organization` (contact point, `sameAs`) on every page, `WebSite` on the landing, `ItemList` of four `Product` entries with a `CHF` `Offer` (the retainer without `price`) on pricing, `AboutPage`, `ContactPage`; no error, warnings noted here → AC-3 (shapes checked locally 2026-09-06: every block parses, the five types and the four ordered `Product` entries are present, the retainer offer carries no `price`; rerun 2026-09-06 after 5c26fdb: the `ItemList` offers now publish `price` 10000, 5000 and 2000 in `CHF` in that order and the retainer offer still carries no `price`, on both `/en/pricing` and `/de/preise`; the `Organization` on every page keeps the placeholder LinkedIn `sameAs` until the owner gives the URL; after the second amendment the `Organization` block on `/en`, `/en/pricing`, `/de/preise` and `/en/contact` carries `@context`, `@type`, `name`, `url`, `logo` and `contactPoint` and no `sameAs` key, and the `ItemList` names read EHS Culture Snapshot, EHS System & Culture Snapshot, Compliance Check, EHS System & Culture Snapshot and EHS Implementation Partner with prices 2000, 5000, 10000 and none; the Schema.org validator (validator.schema.org, the built HTML of `/en`, `/en/pricing`, `/en/about` and `/en/contact` posted 2026-09-06) reports zero errors and zero warnings on all four pages; Google's Rich Results Test needs a public URL and the previews are protected while `VERCEL_AUTOMATION_BYPASS_SECRET` is not in `.env.local`, so it waits for the secret locally or for the first production deploy)
- [x] Open `/sitemap.xml` → eight URLs (four pages, both languages, German slugs) each with alternates; `/robots.txt` on a preview → `Disallow: /` and no sitemap line; on production → the `/*/app`, `/*/expert`, `/*/admin`, `/api` rules plus the sitemap → AC-4
- [x] On `/en` type "Geberit" in the hero field and submit → `/en/sign-up?company=Geberit` with the Company field prefilled; submit the empty field → `/en/sign-up` with no parameter; scroll the page → hero, three proof points, four numbered steps, four package cards linking to pricing, the campaign wall with six pieces (no placeholder frame), the closing call to action with the same field → AC-5
- [x] Open `/en/pricing` and `/de/preise` → four cards in the order EHS Culture Snapshot, EHS System & Culture Snapshot, Compliance Check with both snapshots, EHS Implementation Partner (German names translated); each fixed price card shows the name, promise, the "Best for" line, the price as `CHF` plus digits without decimals (`2'000`, `5'000`, `10'000`) and "excl. 8.1% VAT" / "exkl. 8.1% MWST", a "Start with the free benchmark" link to sign up, then below a hairline the delivery line, three pills and the Output and Outcome rows; the fourth card shows "On demand" / "Auf Anfrage", no VAT note, and "Let's talk" / "Sprechen wir" linking to `/en/contact?topic=retainer` → AC-6 (amended 2026-09-06; proven the same day on the production build served on port 3002: `/en/pricing` and `/de/preise` render the four cards in the order culture, sms, compliance, retainer with `CHF 2’000`, `CHF 5’000`, `CHF 10’000` and the VAT note on the first three, "On demand" / "Auf Anfrage" and "Let's talk" / "Sprechen wir" to `/de/kontakt?topic=retainer` on the fourth, and on every card the best for line, the delivery line, three pills and the Output / Ergebnis and Outcome / Wirkung rows; no "Price on request" or "Preis auf Anfrage" left in the HTML; `e2e/marketing.spec.ts` 23 passed with axe clean on the eight pages in both themes)
- [x] On every marketing page → the header shows Pricing, About, Contact with localized labels and slugs, the current page carries `aria-current="page"` (desktop and the small screen sheet); the footer shows the signature, the tagline, Product (Pricing, Free benchmark), Company (About, Contact, the mailto) and no Legal group → AC-7
- [x] Open `/de/kontakt?topic=retainer` → the contact facts inside an `address` element, the Retainer radio preselected; fill the form → the character count updates live, errors show under the field and in an announced summary with links; submit after three seconds → the confirmation names one working day and links to the landing page; press the browser back button → no resubmission → AC-8 (contact facts rerun 2026-09-06 after 5c26fdb: the `address` element on `/en/contact` and `/de/kontakt` reads "IC Hotz GmbH, Obermühle 5, 6340 Baar" and the mailto; since the second amendment there is no phone row while `SITE.phone` is null: reconfirmed on the production build, the `address` element holds the legal name, the street, the postal code and city and the mailto only, and the only "Phone" string on the page is the form field)
- [x] After the submission → the `enquiries` row holds the trimmed lowercased email, the topic, the locale of the page, a 64 character `ip_hash` (or null without a forwarded header), no organization for a visitor; Slack (or the local catcher) shows "Enquiry received" with the company and "Retainer" and an "Open enquiry" button to `/en/admin/enquiries/<id>`; the acknowledgement arrives in Mailpit within a minute in German naming "Retainer-Anfrage" and one working day → AC-9, AC-14 (proven 2026-09-06 on the local stack: the row reads topic `retainer`, locale `de`, the lowercased address, status `new`, a 64 character `ip_hash`, and null `organization_id` and `submitted_by` for a visitor; the German acknowledgement arrived in Mailpit naming "Retainer-Anfrage" and one working day, and the worker logged `send-email` and `ops-alert` Success. The Slack button URL was not read back, the local catcher was not running.)
- [x] Submit with the hidden `website` field filled (through devtools) → confirmation, no row; submit within one second of the page load → confirmation, no row; a sixth submission from one forwarded address in an hour and a fourth with one email in a day → the alert "Please try again later or write to service@sme24.ch" and no row → AC-10
- [x] Sign in as ops, open `/en/admin/enquiries` → the list defaults to the `new` filter, newest first, received, topic, company, contact, status; the filter select and "all" work; open a row → every field except `ip_hash`, the language name, the organization name for a signed in client's row; set Contacted with a note → saved, `handled_by` and `handled_at` set once; set Closed → both unchanged; an unknown id → not found; a client opening either page → forbidden → AC-12
- [x] Sign in as a client, submit the contact form → the row carries the client's `organization_id` and `submitted_by`; the ops detail shows the organization name → AC-9, AC-12
- [x] Run axe (Playwright `e2e/marketing.spec.ts` and `e2e/enquiries.spec.ts`) → no violation on the four pages in both languages and both themes, on the contact form's error state, on `/admin/enquiries` and its detail; complete and submit the form with the keyboard only; every campaign image has an `alt` → AC-15
- [ ] On the Vercel preview run Lighthouse mobile on the four pages → record LCP, CLS and INP here (targets 2.5 s, 0.1, 200 ms) → AC-16 (measured 2026-09-06 on the local production build, `next start` with Chrome for Testing and the Lighthouse mobile preset, because the preview is protected and the bypass secret is not local: `/` score 74, LCP 4.65 s; `/pricing` 84, LCP 4.64 s; `/about` 94, LCP 3.07 s; `/contact` 90, LCP 3.62 s; CLS 0.000 on all four; TBT 16 to 41 ms, INP has no lab value and needs field data. CLS holds, LCP misses 2.5 s on all four pages. Not ticked: the Follow-up of the spec lists the next cuts in order, `Toaster` and `TooltipProvider` out of the root layout and a CSS only header disclosure, each a spec amendment with a new budget, which is the owner's call; rerun on the preview once the secret is local or on the first production deploy and record the numbers here)
- [ ] Open `/en/pricing` with the network panel → the Sentry chunk (about 168 kB gzipped) is requested only after the `load` event and no module script carries `BrowserTracing`; open `/en/app` signed in → the same chunk is requested at once; on `/en/pricing` a captured error before `load` is not reported (accepted) → AC-16 (amendment) (static half proven 2026-09-06 from the build: no module script of `/en/pricing` contains `BrowserTracing` or `$ZodError`, 17 module scripts scanned, and `pnpm budget` exits 0 on the same rule; the network panel timing on a deployment is still owed)
- [x] Open `/admin/design` → the Marketing section shows the package card, the FAQ and the enquiry form empty and with every error, and the gallery passes axe → AC-15

## Commands
- [x] `pnpm build` → every marketing route prerendered (`●` for `/de-CH` and `/en-CH` under `/`, `/pricing`, `/about`, `/contact`) and the four `opengraph-image` routes listed → AC-1, AC-16 (Next 16 prints no sizes; the measured first load JavaScript is about 415 kB gzipped per page, over the 120 kB budget, see `docs/marketing.md`)
- [x] `pnpm test:db` → `supabase/tests/enquiries.test.sql` passes (anon, client and expert see nothing; ops read and update the four columns only; the service key inserts and deletes; the checks hold; the audit rows) → AC-11
- [x] `pnpm test` → `tests/features/marketing/`, `tests/features/enquiries/`, `tests/trigger/purge-enquiries.test.ts`, `tests/trigger/send-email-enquiry.local.test.ts` (with the local stack) pass; `tests/features/marketing/catalog.test.ts` stays red until the owner's prices and facts replace the placeholders → AC-6, AC-13, AC-17 (rerun 2026-09-06 after 5c26fdb: 997 of 998 pass; the positive price test is green, and the one red test is the `SITE_PLACEHOLDERS` check, which still lists `phone` and `sameAs`; after the second amendment 997 pass and 2 skip with nothing red, `SITE_PLACEHOLDERS` is empty)
- [x] `pnpm build && pnpm budget` → one table with the eight pages, `/de`, `/de/preise`, `/de/ueber-uns`, `/en`, `/en/pricing`, `/en/about` at or under 250 kB and `/de/kontakt`, `/en/contact` at or under 350 kB (gzipped module scripts, the `nomodule` polyfill not counted), no `BrowserTracing` in any module script, no `$ZodError` on the six content pages, exit code 0 (measured 2026-09-06: 221.6, 218.2, 220.6 and 326.3 kB; remeasured the same day after the second amendment's pricing cards: 221.0, 217.7, 220.0 and 325.7 kB, exit 0) → AC-16 (amendment)
- [x] `pnpm budget --url <the Vercel preview URL>` with `VERCEL_AUTOMATION_BYPASS_SECRET` set → the same table within a kilobyte of the local numbers, exit 0; the `e2e.yml` run of the deployment shows the budget step green → AC-16 (amendment) (proven 2026-09-06 by the `e2e.yml` run of the preview of b62cd6d, green with the budget step: 221.6, 218.3, 220.6 and 327.3 kB for the content and contact pages, all `ok`; the three content pages sit within a kilobyte of the local 221.0, 217.7 and 220.0 kB, the contact page 1.6 kB above its local 325.7 kB, the build time public variables inlined on the deployment, still 22 kB under its budget. Two fixes on the way: the slug redirect test now follows the redirect chain because Vercel's bypass cookie adds a self redirect, and the script recognises the `/_next/static/immutable/chunks/` path a deployment serves. The step was not run from this machine, the secret is not in `.env.local`.)
- [x] `pnpm test tests/scripts/bundle-budget.test.ts tests/instrumentation-client.test.ts tests/env.test.ts` → the tag parsing, the page list against the routing tables, the comparison, the deferred Sentry load and the `publicEnv` cases pass → AC-16, AC-17
- [x] `PLAYWRIGHT_BASE_URL=http://localhost:3000 TRIGGER_DEV_RUNNING=1 pnpm test:e2e e2e/marketing.spec.ts e2e/enquiries.spec.ts` with a worker from this checkout → all pass including the Mailpit acknowledgement → AC-8, AC-9, AC-10, AC-12, AC-14, AC-15, AC-17 (28 of 29 green 2026-09-06 after the branch was rebased on origin/main and the `enquiries` migration applied. The one failure is the contact thread's Mailpit assertion, and it is an environment limit, not the feature: the Trigger.dev worker runs in the cloud, so the `EMAIL_SMTP_URL` of `127.0.0.1:54325` is the worker container's own loopback and never reaches this machine's Mailpit. Proven on the way: with `EMAIL_ALLOWED_RECIPIENTS` unset the delivery row is `skipped / not_allowlisted`, and with `@example.test` allowlisted the same row turns `sent` with no error. One acknowledgement did land in Mailpit earlier in the session, subject "Ihre Anfrage ist bei SME24 eingegangen", greeting "Guten Tag Clara Muster", naming "Retainer-Anfrage" and one working day, so the rail itself is proven. Rerun this box where the worker can reach the inbox..Rerun 2026-09-06 by /check verify with a local worker from this checkout: 28 passed, 1 failed, and the cause is now identified and is not the feature. The `send-email` task chose the `resend` transport, not SMTP, because `RESEND_API_KEY` lives in the Trigger.dev project's own environment variables, which the local worker inherits, and `chooseTransport` prefers Resend over `EMAIL_SMTP_URL`. The delivery row for a submission of this run reads `status` `sent`, `transport` `resend` with a real `provider_message_id`, so the acknowledgement did send, to Resend rather than to Mailpit. Mailpit therefore stays empty and the poll times out. To turn this box fully green, unset `RESEND_API_KEY` in the Trigger.dev dev environment so the worker falls back to SMTP.)
- [x] `pnpm trigger:dev` then trigger `purge-enquiries` from the dashboard against rows aged by hand → a row older than 30 days loses `ip_hash`, a closed row handled more than 12 months ago is deleted, a closed row at 11 months stays, the run logs both counts → AC-13 (proven 2026-09-06 by /check verify: four rows seeded at the boundaries and the task triggered against the local worker, run `run_06g7emp0k0rldp4ieun6fd6201` COMPLETED with output `{"hashesCleared":1,"deleted":1}`. The 31 day row lost its `ip_hash`, the 29 day row kept it, the closed row handled 13 months ago was deleted and the closed row at 11 months stayed.)

## Value sourcing
- [x] Switch the language on any page → title, description, `h1` and section copy change with the `locale` route param; the canonical and `hreflang` links follow `PATHNAMES` (German slugs) over `NEXT_PUBLIC_APP_URL`; `og:locale` is `de_CH` or `en_CH` with the other as alternate
- [x] Change `marketing.pricing.og.statement` in one catalog and rebuild → only that language's card changes; remove the key → the card falls back to the site name (exercised 2026-09-06 by /check verify. The change half holds: setting the English key to "CHANGED ONLY IN ENGLISH." and rebuilding put that string in the `og:image:alt` of `/en/pricing` while `/de/preise` kept "Fixpreis. Keine Überraschungen.", so the two languages are independent. The removal half never reaches the runtime fallback: deleting the key fails `pnpm build` at typecheck, because the catalogs are typed and `tests/features/marketing/catalog.test.ts` requires `statement`. The `t.has("statement")` guard in `og-image.tsx` is therefore dead code in practice, which is a stronger guarantee than this step assumed, not a defect. Catalog restored and the build is green.)
- [x] Change `SITE.email` or `SITE.sameAs` → the layout's `Organization` JSON-LD and the contact page follow; change `PACKAGES[].sortOrder` → the pricing cards and the `ItemList` reorder; set a price → `CHF` plus the whole number in the card and `price` in the offer (the price half proven 2026-09-06: setting the three prices in `packages.ts` put `CHF 10’000`, `CHF 5’000`, `CHF 2’000` on the cards and `price` 10000, 5000, 2000 in the offers, and changing `SITE.legalName`, `street`, `postalCode` and `city` moved the contact page's `address` with them; the `email`, `sameAs` and `sortOrder` changes not exercised; the second amendment then exercised `sortOrder` and `sameAs`: reordering `PACKAGES` moved the cards and the `ItemList` to culture, sms, compliance, retainer, and emptying `SITE.sameAs` removed the key from the `Organization` block)
- [x] Type a name with spaces and an umlaut in the hero field → the sign up URL carries it URL encoded and the Company field shows it; a name over 200 characters is cut, never rejected
- [x] Open `/en/contact` without `topic` → General preselected; `?topic=retainer` → Retainer; `?topic=bogus` → General; the prerendered HTML (curl) holds the whole form with General selected
- [x] Submit from `/de/kontakt` → the row's `locale` is `de` and the acknowledgement is German; from `/en/contact` → `en` and English (the German half proven: locale `de` stored and the acknowledgement German; the English half not rerun)
- [x] Load the contact page, wait, submit → the hidden `startedAt` was set on the client after mount (two visitors get different values); curl the page → the HTML carries no `startedAt` value
- [x] Send the form with `x-forwarded-for: 203.0.113.7, 10.0.0.1` → `ip_hash` is the SHA 256 hex of `203.0.113.7`; with only `x-real-ip` → its hash; with neither → null (the forwarded header half proven 2026-09-06 by /check verify: a submission carrying `x-forwarded-for: 203.0.113.7, 10.0.0.1` stored `ip_hash` `fec52565aa0cf18f57d7cf5b3ac728503b8992d2d6f7d46da1d1201090902b02`, which is exactly the SHA 256 of `203.0.113.7`, so the first address wins and `10.0.0.1` is ignored; the `x-real-ip` only and the neither cases are covered by the Vitest branches, not rerun by hand)
- [x] Submit signed in as a client → `organization_id` from the claims and `submitted_by` = the user; as ops or expert → both null
- [x] Seed five rows with one `ip_hash` in the last hour, or three with one email in the last day → the next submission is rate limited; rows older than the window do not count
- [x] Check Slack → `Organization` = the company name, `Topic` = "Retainer" or "General question" (English), the button URL = app URL + `/en` + `/admin/enquiries/<id>`; the run's idempotency key is `enquiry/<id>/alert` (proven 2026-09-06 by /check verify against a local webhook catcher on the host address: the payload reads title "Enquiry received", `Organization` = "Slack Probe AG", `Topic` = "Retainer" in English, `Time` = "06.09.2026, 17:31" in the Swiss format, and the "Open enquiry" button URL = `http://localhost:3000/en/admin/enquiries/9c30a553-f0d6-4ce9-9601-52e4407913a0`, the app URL plus `/en` plus the bare path)
- [x] Check `email_deliveries` → `idempotency_key` = `enquiry/<id>/ack`, `source_event` = `enquiry.received`, `template` = `enquiry_received`, the recipient address and locale from the row, `data` = `{ contactName, topic }`, `recipient_id` null and no `notifications` row (partly seen 2026-09-06 while a run was in flight: `idempotency_key` = `enquiry/<id>/ack`, `source_event` = `enquiry.received`, `template` = `enquiry_received` and the recipient address, status `skipped / not_allowlisted` without an allowlist and `sent` with one. The rows are deleted by the e2e teardown, so `data`, `locale`, `recipient_id` and the absence of a `notifications` row were not read back; check them on a run you keep.. Read in full 2026-09-06 by /check verify on a submission kept out of the e2e teardown: `idempotency_key` = `enquiry/<id>/ack`, `source_event` = `enquiry.received`, `template` = `enquiry_received`, `locale` = `de` from the row, the recipient address, `subject` = "Ihre Anfrage ist bei SME24 eingegangen", `data` = `{"topic": "retainer", "contactName": "Clara Muster"}`, `recipient_id` null, and `status` `sent` over the `resend` transport with a `provider_message_id`. The `notifications` count did not move, so an outside address writes no notification row.)
- [ ] Check PostHog (when the key is set) → event `enquiry_sent` with `distinctId` = the enquiry id and `topic`, `locale`
- [x] Open the acknowledgement → greets by the contact name, names the topic in the recipient's language and "one working day"; the confirmation panel says the same reply time (read in Mailpit 2026-09-06: "Guten Tag Clara Muster", "Danke für Ihre Retainer-Anfrage", "Ein Senior-Experte antwortet innerhalb eines Arbeitstags" and the button back to `/de`; the confirmation panel's reply time was checked by the e2e contact thread assertion.)
- [x] On `/admin/enquiries` → the received time in the `dateTime` format in Swiss time; the language column says "German" or "English" (the alerts registry helper, English only); the organization name on the detail comes from `organizations.name`
- [x] Move a row from `new` to `contacted` as one ops user while another ops user does the same → exactly one `handled_by`; a second change never rewrites it (proven 2026-09-06 by /check verify through the ops detail page: the `new` to `contacted` save set `handled_by` to the ops user and `handled_at` to 15:33:39Z, and the following `contacted` to `closed` save left both untouched while `updated_at` moved on to 15:33:42Z, so the handler is written once. The `.eq("status", "new")` guard in `updateEnquiry` is what makes two ops users racing settle on one handler.)
- [x] Age rows by hand and run the purge → the 30 day and 365 day thresholds (`IP_HASH_RETENTION_DAYS`, `CLOSED_RETENTION_DAYS`) decide, measured from `created_at` and `handled_at` (proven 2026-09-06 by /check verify: rows at 31 and 29 days and closed rows handled 13 and 11 months ago, and only the 31 day row and the 13 month row were touched)
- [x] Change a budget in `BUDGETS_KB` (`scripts/bundle-budget.mts`) below a measured page and rerun `pnpm budget` → that page reads `over` and the exit code is 1; restore it → `ok`
- [x] Add `import "@/lib/env"` to a file under `src/components/` or `src/features/*/ui/` and run `pnpm lint` → refused with "zod must not reach the browser"; the same import in `src/app/**` passes (server code keeps `clientEnv()`)
- [x] Set `NEXT_PUBLIC_POSTHOG_HOST` empty and `NEXT_PUBLIC_SENTRY_DSN` to a space in `.env.local`, rebuild → `publicEnv()` in the browser gives the default host and an undefined DSN (Sentry `enabled: false`), while `clientEnv()` on the server still validates the six values (proven 2026-09-06 by /check verify: with the host emptied and the DSN set to a single space the build stayed green, the browser chunk carries the default host `https://eu.i.posthog.com`, and no `dsn:` string is emitted at all. `optional()` in `src/lib/env.public.ts` trims and maps the empty result to undefined, so `enabled: Boolean(env.NEXT_PUBLIC_SENTRY_DSN)` in `src/instrumentation-client.ts` is false. `pnpm test tests/env.test.ts tests/instrumentation-client.test.ts tests/scripts/bundle-budget.test.ts` passed 44 of 44 and `.env.local` was restored.)
- [x] Open `/de/preise` on a deployment through `pnpm budget --url` → the German slug from `PATHNAMES` is fetched (`/de/preise`), not the route segment; locally the file `de-CH/pricing.html` is read (the deployment run of 2026-09-06 lists `/de/preise` at 218.3 kB, which the script can only measure by fetching that slug; the local table lists the same page from `de-CH/pricing.html`)

## Acceptance-criteria coverage
- AC-1 routes and alternates · AC-2 social cards · AC-3 structured data (Rich Results Test by hand) · AC-4 sitemap and robots · AC-5 landing and prefill · AC-6 pricing (red on placeholders) · AC-7 header and footer · AC-8 contact form · AC-9 action and rails · AC-10 guards · AC-11 pgTAP · AC-12 ops pages · AC-13 purge · AC-14 acknowledgement · AC-15 axe · AC-16 static build, `pnpm budget` at 250 and 350 kB (amendment 2026-09-06), Lighthouse by hand · AC-17 tests
